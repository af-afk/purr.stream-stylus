[package]
name = "purrstreamstylus"
version = "0.1.11"
edition = "2021"
license = "MIT"
homepage = "https://github.com/af-afk/purr.stream-stylus"
repository = "https://purr.stream"
keywords = ["arbitrum", "ethereum", "stylus", "alloy"]
description = "Purr Stream (Rust Stylus edition)"

[dependencies]
alloy-primitives = { version = "=0.8.20", default-features = false, features = ["native-keccak"] }
alloy-sol-types = "=0.8.20"
mini-alloc = "0.8.4"
stylus-sdk = "0.8.4"
tiny-keccak = "2.0.2"

[dev-dependencies]
tokio = { version = "1.12.0", features = ["full"] }
ethers = "2.0"
stylus-sdk = { version = "0.8.4", features = ["stylus-test"] }

[features]
export-abi = ["stylus-sdk/export-abi"]
debug = ["stylus-sdk/debug"]

[[bin]]
name = "purrstreamstylus"
path = "src/main.rs"

[lib]
#name = "libpurrstream"
crate-type = ["lib", "cdylib"]

[profile.release]
codegen-units = 1
strip = true
lto = true
panic = "abort"
opt-level = "s"
